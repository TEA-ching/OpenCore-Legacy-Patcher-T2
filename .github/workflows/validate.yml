name: CI - Validation

on:
  push:
    paths-ignore: 
      - 'docs/**'
  workflow_dispatch:
  release:
    types: [published]

jobs:
  build:
    name: Validate
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v4
      - name: Validate
        run: /Library/Frameworks/Python.framework/Versions/3.13/bin/python3 OpenCore-Patcher-GUI.command --validate
